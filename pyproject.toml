[project]
name = "dict"
version = "0.1.0"
description = "Penetration testing wordlist capability repository"
requires-python = ">=3.9"
dependencies = [
    "jsonschema>=4.21",
]

[tool.uv]
package = false
